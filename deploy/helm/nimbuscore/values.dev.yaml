ingress:
  hosts:
    - host: api.dev.nimbuscore.io
      paths:
        - path: /
          pathType: Prefix

config:
  logLevel: debug
  oidc:
    issuer: http://host.docker.internal:5556/dex
    clientId: nimbuscore
    redirectUrl: http://nimbuscore.local/auth/callback
  rabbitmq:
    url: amqp://user:changeme@rabbitmq:5672
  ingress:
    host: nimbuscore.local

monitoring:
  serviceMonitor:
    enabled: false

api:
  replicas: 1
  autoscaling:
    enabled: false

operator:
  replicas: 1

workspaceManager:
  replicas: 1

postgresql:
  enabled: true
  auth:
    username: nimbuscore
    password: nimbuscore
    database: nimbuscore
  primary:
    persistence:
      size: 5Gi
    initdb:
      scriptsConfigMap: nimbuscore-db-init

redis:
  enabled: true
  auth:
    password: nimbuscore
  master:
    persistence:
      size: 2Gi

rabbitmq:
  enabled: false
  auth:
    username: user
    password: changeme
